

Problem1 : run the python file HistogramEQ.py by changing the path file.

the input is for the partial histogram equalization. given 1 for full Histogram equalization and inbetween 0 and 1 for partial.
alpha = 0.8 means 80% of histogram image and 20% of original image.

Problem 2 : run the file Lanedetection.py by changing the path file.

Problem 3 : Run the file TurnPredict.py by changing the path file.

This will show the output of the Schannel image of HLS color channel used for pipeline.

For a gray image implementation, uncomment the line 61,62 and comment the line 64 and run the file.

The line 61 and 62 are the gray and edge image of the RGB image provided.


